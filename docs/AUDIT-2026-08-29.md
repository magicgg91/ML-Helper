# Audit de conformité — repo vs cahier des charges & brief de démarrage

**Date :** 29/08/2026
**Portée :** intégralité des 4 priorités demandées (P1 à P4), aucune n'a été survolée — voir méthode ci-dessous.
**Méthode :** audit en lecture seule, découpé en 4 sous-agents parallèles indépendants (un par priorité), chacun ayant lu intégralement `AGENTS.md`, `docs/cahier-des-charges-ml-helper.md` (le « cdc ») et `docs/brief-demarrage-codex.md` (la « brief »), puis vérifié le **code source réel** (pas seulement les tests, pas seulement les valeurs par défaut TypeScript — les migrations Prisma/valeurs seedées en base ont été lues directement quand pertinent). Ce document fusionne leurs 4 rapports en un seul, en dédupliquant les constats qui se recoupaient d'une priorité à l'autre (renvois croisés `→ voir P.X` plutôt que répétition).

**Un seul bug flagrant a été trouvé et corrigé pendant l'audit**, conformément à l'exception explicitement autorisée pour le cadre de cette mission (voir Priorité 1, section 🔧). Aucune autre modification de code n'a été faite — tout le reste de ce document est un constat, pas une correction.

---

## 🔄 Mise à jour — 29/08/2026, après remplacement du cdc et de la brief

Le porteur de projet a fourni des versions à jour du cdc (2392 lignes, vs 1940 dans le repo au moment de l'audit initial) et de la brief (606 lignes, vs 230) — **la version présente dans le repo GitHub au moment de l'audit initial était bien obsolète sur plusieurs points**, confirmant l'hypothèse. Les deux fichiers ont été remplacés dans `docs/` par les versions fournies. Chaque constat du rapport ci-dessous a été revérifié contre le nouveau texte ; les entrées concernées ont été mises à jour **en place** (catégorie changée si résolu, texte affiné si partiellement résolu) plutôt que dupliquées — cette section résume seulement ce qui a changé de statut.

**Résolu par la mise à jour documentaire (le code était déjà correct, c'est le cdc qui avait pris du retard) :**
- Coût de fusion Terradust (Expédition) — le nouveau cdc verrouille exactement 600/1100/2000/4000/8000, identique au code → passé en ✅.
- Incréments par étoile Expédition — le nouveau cdc confirme désormais 10/10 stats (à la rareté Légendaire), contre 2/10 dans l'ancien texte → affiné en ✅, avec une nuance plus étroite qui subsiste (voir entrée mise à jour).
- « Comparaison de stuff » — le nouveau cdc confirme explicitement la suppression volontaire (« entièrement retiré — code, route, entrée admin, migration DB ») → passé en ✅.

**Toujours d'actualité malgré la mise à jour documentaire (donc de vrais écarts, pas un simple retard de doc) :**
- Classement — Bronze/Or toujours vides dans le code, le nouveau cdc affirme toujours « TOUTES CONFIRMÉES » (ligne 1025 du nouveau texte) sans changement sur ce point précis.
- Level Up — Argent toujours marqué « reste à vérifier » dans le nouveau cdc (aucune donnée reçue), l'absence de champ admin dédié reste donc un vrai écart à la règle AGENTS.md.
- Coût de fusion en Pouciel (Combat) — le nouveau cdc reconfirme la formule verrouillée (K=20/40/80/160/320) mais ne mentionne aucune implémentation prévue ; toujours absent du code.
- Bug Platine (+2 au lieu de +1 point/niveau) — indépendant des docs, déjà corrigé (commit `bdec46f`).

**Affiné (ni résolu ni confirmé à l'identique, situation plus précise) :**
- Les 30 lignes « Inconnu » d'équipement de combat — le nouveau cdc confirme désormais aussi Shopkeeper et Soldier (en plus de Bard/Journeyman/Thief/Adventurer/Knight déjà connus), et **corrige** la valeur de Thief (Récupération 2% → Recycleur 1%, désormais confirmée en jeu sur les 3 emplacements). Il ne reste donc plus que **3 sets réellement inconnus** (Smuggler, Royal Archer, Royal Guard = 9 lignes), contre 15 dans l'ancien texte — mais **21 lignes** (7 sets) sont maintenant des données confirmées à reporter dans `equipment-data.ts`, pas seulement 15.

---

## Priorité 1 — Formules et données de jeu

### ✅ Conforme

- **Villes — VP** : `VP(n) = 20 × 1,115^(n−1)`, universel, seedé identique en base pour les 6 ligues — `prisma/migrations/20260823010000_load_city_parameters/migration.sql:13` (`"vp":{"base":20,"ratio":1.115}`), implémenté `src/lib/city-calculators.ts:17-22` (`vpAt`).
- **Villes — Remparts** : `70 × 1,2^(n−1)`, seedé identique, `src/lib/city-calculators.ts:24-31` (`wallAt`).
- **Villes — Coût d'upgrade** : niveau 1=0, niveau 2=base, niveau≥3=`base×ratio^(n−2)`, base=10/ratio=1,2 — `src/lib/city-calculators.ts:33-41` (`upgradeCostAt`), valeurs seedées identiques.
- **Villes — Multiplicateurs Army/Gold par ligue** : table seedée (`prisma/migrations/20260823010000_load_city_parameters/migration.sql:13`) et fallback (`src/lib/city-parameters.ts:15-19`) correspondent exactement à cdc L.636-649 (Bronze 2/5, Argent 2,25/6,25, Or 2,75/8,75, Platine 2,75/8,75, Diamant 3/10, Légende 3/10).
- **City Max Level** : recherche itérative codée en dur (`src/lib/city-calculators.ts:81-100`, `maximumReachableLevel`), pas de `formula_params` associé — conforme à la décision cdc L.597 (« Option B — codé en dur, non éditable dynamiquement »).
- **City Cost / City Production** : réutilisent le même jeu de paramètres via `cityStatsAt`/`cumulativeCostAt` (`src/lib/city-calculators.ts:43-67`), pas de duplication.
- **Templiers — coût** : `Coût(n) = arrondi(150 × 1,3^(n−1))`, seedé `prisma/migrations/20260822000000_templar_formula/migration.sql:4` (base=150, ratio=1,3), implémenté `src/lib/templar-parameters.ts:14-17`.
- **Templiers — bonus par stat** : Attaque/Def +0,25%, Recruteur/Or +0,5%, Speed +1% — `src/lib/gems-templars.ts:127-131`, exact.
- **Level Up — XP universelle** : `XP(n→n+1) = 50 × 1,3^(n−1)`, seedé `prisma/migrations/20260824010000_level_up_reference/migration.sql:5`, implémenté `src/lib/level-up.ts:80-82`.
- **Level Up — Argent non fabriqué** : `levelUpTroopsAt` renvoie explicitement `null` pour `league === "silver"` (`src/lib/level-up.ts:74`) — aucune valeur inventée, conforme à cdc L.947-948 (« reste à obtenir »). *(Mais voir l'écart correspondant en Priorité 4 — l'absence de champ admin pour cette valeur est un problème distinct.)*
- **Level Up — cycle des 5 coffres** : motif `Coffre→Urne→Coffret à bijoux→Jarre→Caisse` exact sur les paliers 10-60 (`src/lib/level-up.ts:84-94`, `messages/fr.json:578-584`), conforme cdc L.941-943.
- **Level Up — 3 groupes de formules distincts** : Bronze/Diamant/Légende (32,2028×1,245^n), Platine (35,88×1,237^n), Or (32,49×1,24^n) bien séparés dans `src/lib/level-up.ts:23-36`, cohérent avec cdc L.868-886.
- **Gemmes — prix par ligue** : Argent 3000 → Légende 7000, seedé et fallback (`src/lib/gem-parameters.ts:72-78`), conforme cdc L.1531.
- **Gemmes — table 10×6 `y`** : vérifiée cellule par cellule dans `src/lib/gem-parameters.ts:17-71` contre cdc L.1580-1592, aucun écart.
- **Ranking — arrondi floor** : `Math.floor(...)` utilisé (`src/lib/ranking.ts:143`), jamais `Math.round` — la tâche historique « A: Ranking floor rounding » semble donc déjà traitée dans le code actuel malgré son statut « pending » dans le suivi de tâches de cette session.
- **Ranking — Argent/Diamant/Légende** : seuils, mouvements et récompenses exacts contre cdc (`src/lib/ranking.ts:38-97` vs cdc L.778-808).
- **Équipement de combat — 10 incréments par étoile** : `src/lib/equipment.ts:69-80` exact contre cdc L.1342-1351 (Attaque/Charognard/Intrépide/Bravoure +2, Défense/Prospérité/Recruteur +3, Vitesse +5, Recycleur/Récupération +1).
- **Bonus de base du temple (clan)** : `src/lib/player-settings.ts:137-143` exact contre cdc L.679-685.
- **Skill points par niveau — règle générale** : `points_totaux = (niveau−1) × points_par_niveau(ligue)`, `src/lib/player-settings.ts:167-172`, conforme cdc L.1151.
- **[Ajouté après remplacement du cdc] Équipement d'Expédition — coût de fusion en Terradust.** `defaultExpeditionMergeCostBase = {Commun:600, Rare:1100, Épique:2000, Mythique:4000, Légendaire:8000}` (`src/lib/reference-equipment.ts:138-144`) correspond exactement au nouveau cdc (« Coût de fusion désormais 100% verrouillé, les 5 raretés confirmées », nouveau cdc L.1697-1709) — la version du cdc présente dans le repo au moment de l'audit initial était simplement obsolète sur ce point (elle listait encore ce coût sous « reste à définir »).

### ⚠️ Écart

- **Équipement de combat — coût de fusion en Pouciel : fonctionnalité absente, pas juste une valeur fausse.** Le cdc verrouille `Coût_Pouciel(rareté, n) = K(rareté) × 2^(n−1)`, K = Commun 20 / Rare 40 / Épique 80 / Mythique 160 / Légendaire 320 (cdc L.1246-1266, « entièrement verrouillée »). Aucune fonction de ce type n'existe dans le code. La seule table à 5 valeurs nommée « Skydust » (`defaultCombatSkydustBase`, `src/lib/reference-equipment.ts:179-185` = 3/10/30/120/160) est en réalité la donnée **« Pouciel à la destruction »** de cdc L.1202-1208 — une donnée différente, utilisée uniquement pour une colonne d'affichage (`src/app/api/admin/guides/references/combat-equipment/route.ts:37`), jamais pour calculer un coût de fusion. Contrairement à l'Expédition qui a bien `expeditionMergeCost()` (`src/lib/reference-equipment.ts:222-230`), aucun équivalent `combatMergeCost` n'existe nulle part dans `src`.
- **[Mis à jour après remplacement du cdc] Équipement de combat — 30 lignes « Inconnu » : le compte était bon, la composition ne l'était pas.** Sur ces 30 lignes, **21 (7 sets)** correspondent à des sets que le nouveau cdc confirme désormais : Bard, Journeyman, Thief, Adventurer, Knight (déjà connus dans la version précédente du cdc) **plus Shopkeeper et Soldier**, nouvellement confirmés (nouveau cdc L.1472-1479). Le nouveau cdc **corrige aussi** la valeur de Thief : Récupération 2% (ancien texte) → **Recycleur (Salva) 1%**, désormais confirmée en jeu sur les 3 emplacements (nouveau cdc L.1474). `src/lib/equipment-data.ts` (lignes 2554-2911) n'a reçu aucune de ces mises à jour, garde `skill_1: "Inconnu"` partout. Il ne reste donc que **3 sets réellement inconnus** au sens du cdc actuel — Smuggler, Royal Archer, Royal Guard (9 lignes, nouveau cdc L.1489-1490) — contre 15 supposés à l'audit initial. Le champ reste éditable (pas un blocage AGENTS.md), mais la valeur par défaut documentée dans le code est **périmée sur 21 lignes**, pas juste « en attente ».
- **Équipement de combat — coût de fusion en Pouciel : fonctionnalité toujours absente après vérification contre le cdc à jour.** Le nouveau cdc reconfirme la formule verrouillée (`Coût_Pouciel(rareté, n) = K(rareté) × 2^(n−1)`, K=20/40/80/160/320, nouveau cdc L.1498-1515) sans mentionner nulle part une implémentation prévue ou en cours pour ce calcul précis. Aucune fonction de ce type n'existe dans le code — la seule table à 5 valeurs nommée « Skydust » (`defaultCombatSkydustBase`, `src/lib/reference-equipment.ts:179-185` = 3/10/30/120/160) est en réalité la donnée **« Pouciel à la destruction »**, une donnée différente, utilisée uniquement pour une colonne d'affichage. Contrairement à l'Expédition qui a bien `expeditionMergeCost()` (`src/lib/reference-equipment.ts:222-230`, et son propre tableau public par rareté), aucun équivalent `combatMergeCost` ni tableau associé n'existe nulle part dans `src`. **Toujours un vrai écart**, indépendant de la fraîcheur des docs.
- **[Affiné après remplacement du cdc] Équipement d'Expédition — les 10 incréments par étoile sont désormais confirmés par le cdc (à la rareté Légendaire), mais le code ne distingue toujours pas cette nuance de rareté.** Le nouveau cdc confirme les 10 stats à la rareté Légendaire (nouveau cdc L.1678-1691 ; contre seulement 2/10 dans la version précédente du cdc) — l'écart initialement signalé (« 8 stats sur 10 non confirmées ») est donc en grande partie résolu : le code (`defaultExpeditionStarIncrements`, `src/lib/reference-equipment.ts:76-87`) reflète déjà correctement ces 10 valeurs, et un commentaire du code (lignes 72-75) documente honnêtement la nuance restante. **Ce qui subsiste** : le nouveau cdc précise que la généralisation de ces incréments aux 4 autres raretés (Commun/Rare/Épique/Mythique) n'est **pas elle-même confirmée** (« hypothèse par défaut = même incrément, à corriger en admin si écart constaté », nouveau cdc L.2022, L.1784) — or `ExpeditionStarIncrements` est un type `Record<stat, number>` **sans dimension rareté** (`src/lib/reference-equipment.ts:69`), donc le code ne peut structurellement pas distinguer « confirmé en Légendaire » de « extrapolé sur les 4 autres raretés » — voir le détail fonctionnel (absence d'indicateur dans le simulateur) en Priorité 2.
- **Level Up — précision du coefficient Bronze/Diamant/Légende.** Le cdc écrit littéralement `32,2 × 1,245^n` (cdc L.871 dans les deux versions) ; le code/la valeur seedée est `32.2028` (`prisma/migrations/20260824010000_level_up_reference/migration.sql:5`, `src/lib/level-up.ts:26,29,30`). Écart minime (~0,009%), vraisemblablement une valeur de régression plus précise que le nombre arrondi écrit dans le cdc — mais ce n'est littéralement pas le chiffre du texte. *(Inchangé par le remplacement du cdc.)*
- **Ranking — Bronze et Or totalement vides, Platine incomplet, malgré un cdc qui affirme « TOUTES CONFIRMÉES ».** *(Reconfirmé à l'identique contre le cdc à jour — ce point n'était pas un problème de fraîcheur documentaire.)* Le cdc (nouveau texte L.1025 et environs, même contenu que l'ancien) affirme que les 6 ligues ont leurs seuils/récompenses confirmés, avec le détail complet donné pour Bronze et Or, et un seuil Platine explicitement corrigé (75% ajouté). Dans le code, `defaultRankingConfig.bronze = []` et `.gold = []` (`src/lib/ranking.ts:37,76`) — tableaux vides, rien saisi — et `platinum` n'a que 5 seuils sur 6 (`[1,6,15,50,100]`, le 75% toujours absent), chaque ligne avec `movement: null, league: null, rewards: []`. Aucune migration ne seed de `reference_tables` pour la clé `ranking_leagues` : ce n'est pas un simple filet de secours, c'est ce qui tourne réellement en base tant qu'un admin n'a rien saisi. *(Corroboré indépendamment par les Priorités 2 et 4 — voir listes consolidées.)*

### 🔧 Bug flagrant corrigé

- **Points de compétence par niveau, ligue Platine** — `src/lib/player-settings.ts:54`. Avant : `platinum: 2`. Après : `platinum: 1`. Justification : le cdc (L.1146-1150) est sans ambiguïté — « Bronze / Argent / Or / Platine → +1 point par niveau de Lord ; Diamant / Légende → +2 points » — avec la précision explicite « Platine confirmé également ». Le code attribuait à tort 2 points/niveau aux joueurs Platine, doublant leur budget de points de compétence partout où cette constante est utilisée (Paramètres du joueur, aperçu « reskill full-prod » de Production). Vérifié : aucun test n'attendait la valeur erronée ; les 4 suites concernées (`player-settings.test.ts`, `city-calculators.test.ts`, `city-calculators.test.tsx`, `player-settings-panel.test.tsx` — 67 tests) passent intégralement après correction, ainsi que la suite complète (608/608). **Commit `bdec46f`**, poussé sur `claude/ml-helper-audit-bloc-6-x2g7mv`.

### Non vérifié

- **Algorithme d'optimisation des gemmes** (répartition U/emplacements en mode Optimisation, puissances de 2 en mode Budget) — logique non retracée pas à pas dans `src/lib/gems-templars.ts` au-delà des taux de base, trop complexe pour ce passage.
- **Table brute des 180 lignes d'équipement de combat** (`src/lib/equipment-data.ts`) — seul le motif « Casque=Gantelet=Bottes » et le comptage global des lignes « Inconnu » ont été vérifiés, pas une comparaison champ-par-champ exhaustive contre le cdc.

---

## Priorité 2 — Complétude fonctionnelle

### ✅ Conforme

- **Villes — 3 outils, 1 point d'édition partagé** : Coût de Ville / Niveau Max / Production existent sous `/tools/villes`, statuts `active` indépendants par slug (`src/lib/calculator-catalog.ts:2-13`), mais un seul écran d'édition partagé `/admin/tools/city-parameters` (`src/lib/admin-tools.ts:13-24`) — conforme à l'exigence « pas de duplication d'édition » (cdc L.179).
- **Production — case « reskill full-prod »** : bien présente et toujours affichée (`src/components/city-calculators.tsx:526-546`, `src/lib/city-calculators.ts:127,166`), cohérente avec le prototype de référence.
- **Combat — Taux de gain d'XP et Troupes attaque démo : déjà construits**, contrairement à ce que la brief (rédigée à un stade antérieur) laisse penser (« prêts à être envoyés comme tâche Codex »). Route `/tools/combat`, `src/components/combat-calculators.tsx`, formules dans `src/lib/combat-calculators.ts:8-104` — paliers XP et pourcentages attaque démo exacts contre cdc L.965-971 et L.1000-1007, tous deux éditables en admin, tous deux avec gating actif/inactif conforme (onglet grisé, pas retiré).
- **Compétences — Simulateur de Stuff, Gemmes (2 modes), Templiers** : tous présents et conformes structurellement (4 blocs Attaque/Défense/Or/Vitesse pour Stuff, Optimisation+Budget pour Gemmes avec Bronze exclu de l'achat, formule à 2 paramètres pour Templiers).
- **Simulateur d'Équipement d'Expédition** : existe et est plus avancé que ce que cdc/brief documentent encore comme non construit — `src/components/expedition-equipment-tools.tsx`.
- **Référentiels — Équipements de Combat, Level Up, Templiers, Gemmes** : tous accessibles, `Calculator`/`reference_table` bien distincts en base (colonne Type dans `/admin/guides`), routes `/guides/referentiels/*` fonctionnelles, exclus de `/admin/tools` (`referenceToolSlugs`, `src/lib/admin-tools.ts:6-12`).
- **Liens croisés obligatoires** : le lien « Voir le référentiel complet » existe littéralement dans le Simulateur de Stuff (`src/components/equipment-tools.tsx:497-499`) et le Simulateur d'Expédition (`src/components/expedition-equipment-tools.tsx:359-361`), et au-delà du minimum requis par le cdc, également sur Gemmes et Templiers.
- **D.1 — Statuts actifs indépendants (point historiquement fragile)** : confirmé structurellement sain. `Calculator.active` est un booléen unique par ligne/slug (`prisma/schema.prisma:29-42`), et deux endpoints PATCH totalement séparés (`/api/admin/tools/[id]` par id, `/api/admin/guides/references/[slug]/active` par slug) opèrent chacun sur sa propre table, sans aucun couplage.
- **D.2 — Bouton retour contextuel (point historiquement fragile)** : confirmé réellement contextuel, pas figé — un query param `?from=guides` (`src/lib/admin-tools.ts:30,37`) est propagé et consommé par `src/app/admin/tools/[id]/page.tsx:58-65,83-89` pour choisir entre `/admin/guides` et `/admin/tools` selon la provenance et les droits du rôle courant.
- **D.3 — Filtres de tuiles Combat/Expédition (point historiquement fragile, corrigé plusieurs fois par le passé)** : état final confirmé propre. Familles et raretés démarrent toutes sélectionnées, désélectionner un filtre retire réellement les tuiles du DOM (`.filter(matchesFilters)`), et **aucune classe CSS dim/highlight résiduelle** ne subsiste dans `globals.css` pour ce mécanisme.
- **[Ajouté après remplacement du cdc] « Comparaison de stuff » — suppression confirmée volontaire, pas un bug.** Le nouveau cdc documente explicitement la décision (nouveau cdc L.1247, L.1579 : « Comparateur d'Équipement de Combat : supprimé entièrement — code, route, entrée admin, migration DB. Décision produit : redondant avec le Simulateur d'Équipement de Combat, pas assez utilisé pour justifier le maintien. »). L'état du code (aucune trace, migration de suppression dédiée) correspond exactement à cette décision — la version du cdc/brief présente dans le repo au moment de l'audit initial était simplement en retard sur cette décision produit.

### ⚠️ Écart

- **Classement — 3 ligues sur 6 sans aucune donnée, malgré un cdc « TOUTES CONFIRMÉES ».** *(Reconfirmé contre le cdc à jour — voir Priorité 1 pour le détail complet, ce n'était pas un problème de fraîcheur documentaire.)* Confirmé indépendamment depuis l'angle « le calculateur fonctionne-t-il tel que spécifié » : non, pour Bronze/Or (aucun seuil affichable) et partiellement pour Platine.
- **[Affiné après remplacement du cdc] Équipement d'Expédition — le simulateur n'indique jamais que l'extrapolation aux raretés non-Légendaire n'est pas confirmée.** Le nouveau cdc confirme désormais les 10 incréments par étoile (à la rareté Légendaire — voir Priorité 1), donc l'écart initial (« 8 stats sur 10 présentées comme confirmées à tort ») est en grande partie résolu : ce ne sont plus des hypothèses de valeur par stat, mais une extrapolation par rareté, elle-même explicitement documentée comme non confirmée par le nouveau cdc (« l'extrapolation aux raretés non-Légendaire n'étant pas elle-même confirmée », brief L.411). **Ce qui reste un vrai écart** : le **simulateur réel** d'Équipement d'Expédition (`src/components/expedition-equipment-tools.tsx`, sélecteur 1-8★, calcul via `computeExpeditionTotal`, `src/lib/expedition-equipment.ts:129`) ne fait aucune différence entre une pièce Légendaire (incréments vérifiés) et une pièce Commune/Rare/Épique/Mythique (incréments extrapolés) — recherche exhaustive de `confirmed`/`unconfirmed` dans ce composant : zéro occurrence, et `ExpeditionStarIncrements` n'a de toute façon pas de dimension rareté pour porter cette information (voir Priorité 1). Un joueur planifiant la progression d'une pièce non-Légendaire ne peut donc pas savoir que la courbe qu'il voit est une extrapolation.

### 🔧 Bug flagrant corrigé

_(aucun signalé par cette priorité — les deux écarts ci-dessus demandent un choix de données/design plutôt qu'une correction évidente d'une ligne.)_

### Non vérifié

- **Comportement réel du sélecteur de ligue synchronisé** (position, alignement automatique au chargement depuis localStorage, Bloc 9/point 23) — câblage du code confirmé (`useSyncedLeague` présent sur les 3 calculateurs Villes et Combat), mais pas rejoué dans un navigateur réel.
- **Liste blanche de compétences du Simulateur de Stuff, comparaison exhaustive** — structure générale conforme, mais pas de vérification entrée-par-entrée contre le tableau cdc (L.1297-1304).
- **Exactitude complète des 300 lignes de données brutes d'équipement** — seul le total et les lignes « Inconnu » ont été comptés, pas une vérification champ-par-champ de chaque set.

---

## Priorité 3 — Cohérence UI/UX transverse

### ✅ Conforme

- **Accent violet partagé public/admin** : un seul jeu de tokens CSS (`--accent`/`--accent-strong`, `src/app/globals.css:39-40,73-74`), réutilisé identiquement côté public et dans `/admin/*` — l'admin (shadcn/Tailwind, `src/app/admin/admin.css:1-35`) alias explicitement ses propres tokens (`--color-primary`, `--color-ring`) sur les mêmes variables, pas de palette parallèle.
- **Rareté Légendaire — `--rarity-legendaire` correctement isolée** des 4 autres raretés, utilisée de façon cohérente partout (`rarityClassName`, plusieurs composants).
- **Ordre des familles Combat (Attaque, Défense, Or, Troupes/Vitesse)** cohérent aux 4 points de contrôle : filtres publics, tuiles du référentiel, dropdown admin, blocs du Simulateur de Stuff **et** du mode Optimisation des Gemmes.
- **Ordre des familles Expédition (Or, Équipement, Consommables, Troupes)** cohérent aux mêmes points de contrôle.
- **Formatage des grands nombres** : `formatGameNumber` (`src/lib/city-calculators.ts:182-204`) reproduit fidèlement les seuils k/M/G/T/P et la règle de bascule au palier supérieur dès 999,99.
- **Pourcentages jamais compactés** : `formatPercent` est un chemin de code totalement séparé de `formatGameNumber`, confirmé par grep sur toute la base.
- **Exception budget saphirs honorée** : pas de sélecteur d'unité sur ce champ précis (`src/components/skills-calculators.tsx:560-568`).
- **Mécanisme de repli EN si traduction manquante — existe réellement** : `mergeMessages` (`src/i18n/config.ts:13-30`) fusionne récursivement, jamais de texte vide.
- **Aucun texte codé en dur trouvé dans l'admin** : recherche systématique sur 15 fichiers/écrans admin distincts, zéro résultat — tout passe par `useTranslations`.
- **Aucune formule/paramètre exposé côté public** : le seul composant affichant des libellés `base`/`ratio` (`NamedParametersEditor`) n'est monté que sous `/admin/*`.
- **Parité fr.json/en.json quasi totale** : une seule clé orpheline détectée (voir écart ci-dessous) sur ~980 clés.

### ⚠️ Écart

- **`--gold` utilisé hors du périmètre que son propre commentaire définit.** `src/app/globals.css:13-15` documente explicitement que cette variable est réservée aux données de jeu « genuinely Legendary » — jamais un accent d'interface générique. Or `filterButtonColor()` (`src/lib/game-images.ts:55-57`) renvoie `var(--gold)` pour le bouton de filtre de la famille d'équipement **« Or »**, qui n'a aucun rapport avec la rareté Légendaire (une famille existe à toutes les raretés). Par ailleurs, la couleur canonique Prospérité/Or du cdc (`#c9a04a`, cdc L.1472) est dupliquée en dur ailleurs (`equipmentSkillColors.Prospérité`, `game-images.ts:23`) sans passer par le thème clair/sombre, alors que `--gold` change de valeur selon le thème (`#c9a04a` sombre / `#9b6f1e` clair) — deux représentations différentes de la même couleur « Or » pour le même concept, qui divergent visiblement en mode clair.
- **Clé de traduction orpheline** : `en.json` a `Navigation.admin: "Admin area"` (`messages/en.json:419`), absente de `fr.json`. Le repli fonctionnerait (affichage EN), mais contredit l'exigence de structure de clés identique. Semble être une clé morte (seul usage trouvé : un test, aucun composant de production).
- **Prix des gemmes affichés en valeur brute, sans exception cdc explicite trouvée pour ce cas précis.** `src/components/gems-reference.tsx:51-57` affiche le prix arrondi sans compaction k/M/G, justifié par un commentaire interne (« values stay at most 4 digits, compaction only hurts readability here »). C'est plausible et cohérent avec l'esprit du cdc, mais contrairement à l'exception du sélecteur d'unité (explicitement écrite en L.218), aucune exception textuelle équivalente n'a été retrouvée pour cette table précise dans le cdc — décision de développement non tracée dans le document, pas nécessairement fautive.

### 🔧 Bug flagrant corrigé

_(aucun signalé par cette priorité.)_

### Non vérifié

- **Suppression systématique des zéros décimaux dans `formatGameNumber`** (`5000` → `"5k"` plutôt que `"5.00k"`) : le cdc écrit littéralement `X.XXk` mais son propre exemple (`12.4k`) montre déjà un zéro de fin supprimé — impossible de trancher si c'est une intention du cdc ou une extension du développeur sans demander au porteur de projet.
- **Couverture exhaustive du texte admin au-delà des 15 écrans explorés** — le spot-check est large mais pas une relecture ligne à ligne de chaque fichier admin ; un texte construit dynamiquement (concaténation, template littéral) pourrait échapper aux motifs de recherche utilisés.
- **`.diff-negative` en hex codé en dur** (`src/app/globals.css:976-979`) — classe non référencée par aucun composant actuel (CSS mort probable, ou hook prévu pour une fonctionnalité future comme le comparateur de stuff aujourd'hui supprimé) — impact actuel nul, statut réel non tranché.

---

## Priorité 4 — Données encore non confirmées, éditabilité admin

### ✅ Conforme

- **Level Up — coefficients Troupes par ligue (Bronze/Or/Platine/Diamant/Légende)** : éditables (champs numériques coefficient/ratio par ligue), voir `src/lib/level-up.ts:23-36` et l'éditeur `src/components/named-parameters-editor.tsx:211-328`.
- **Équipements de Combat — 180 lignes** (dont 9 réellement encore manquantes après remplacement du cdc, voir mise à jour P1/P4 — 3 sets, Smuggler/Royal Archer/Royal Guard) : éditables via une vraie table admin (dropdowns famille/rareté/emplacement/arme/compétence, champ numérique libre pour le %), défaut = `"Inconnu"` (placeholder explicite, rien d'inventé) — `src/lib/equipment-data.ts:2758-3064`, éditeur `src/components/reference-admin-editors.tsx:175-235`.
- **Équipement d'Expédition — 10/10 incréments par étoile exposés en admin**, pas seulement les 2 confirmés — `src/lib/reference-equipment.ts:76-87`, éditeur `ExpeditionIncrementsAdmin` (`src/components/reference-admin-editors.tsx:376-410`), avec un commentaire de code citant explicitement la règle AGENTS.md.
- **Équipement d'Expédition — coût de fusion Terradust et Terradust-à-la-destruction** : tous deux éditables par rareté (`ExpeditionMergeCostAdmin`, `ExpeditionDismantleAdmin`).
- **Gemmes — valeurs `y` par compétence×ligue** : stockées et éditées comme **lookup_table par cellule** (choix documenté explicitement en commentaire de code : « le prototype fait foi, un modèle factorisé ne peut pas exprimer une correction isolée par cellule »), 100% éditable — `src/lib/gem-parameters.ts:8-79`.
- **Simulateur d'achat de consommables** : confirmé absent du code — cohérent avec cdc et brief (« reste à définir », hors périmètre), pas une omission.

### ⚠️ Écart

- **Level Up — Troupes Argent : ni valeur, ni champ éditable, contrairement à la règle AGENTS.md.** Le cdc dit « reste à obtenir » (L.947-948), et le code refuse honnêtement d'inventer une valeur (`levelUpTroopsAt` renvoie `null` pour Argent, `src/lib/level-up.ts:74` — voir ✅ en Priorité 1). Mais le type `LevelUpParameters["troops"]` exclut explicitement `"silver"`, et l'éditeur admin (`src/components/named-parameters-editor.tsx:282,323-324`) n'affiche qu'une ligne de texte « Argent — non confirmé », **sans aucun champ de saisie**. La règle AGENTS.md est pourtant sans ambiguïté : « toute donnée encore marquée non confirmée reste éditable en admin avec sa valeur actuelle par défaut, même une hypothèse ». Ici, il n'y a ni champ, ni valeur par défaut, même approximative — refuser d'inventer une donnée de jeu est correct, mais refuser de donner à l'admin un moyen de la saisir dès qu'elle sera connue ne l'est pas.
- **Ranking — contradiction confirmée entre cdc, brief et code, chacun disant une chose différente.** Le cdc (L.843-853) affirme les 6 ligues « TOUTES CONFIRMÉES ». La brief (L.83) dit « Bronze/Or/Platine partiels ». Le code est **encore plus incomplet que ce que dit la brief** : `bronze: []` et `gold: []` sont des tableaux **vides** (pas juste incomplets), et `platinum` a 5 seuils sur 6 avec `movement`/`league`/`rewards` tous `null` — un commentaire du code dit explicitement que la récompense Platine « isn't confirmed yet », contredisant frontalement le cdc. Point positif : l'éditeur admin (`RankingAdminEditor`, `src/components/ranking-admin-editor.tsx`) est déjà la vraie UI dédiée par ligue demandée par le brief (Bloc 3, ajout/suppression de lignes, dropdowns mouvement/ligue cible) — donc le point d'architecture est réglé, il ne manque que la saisie des données Bronze/Or/Platine. *(Voir aussi Priorités 1 et 2 pour les angles « formule verrouillée » et « fonctionnalité incomplète » du même constat.)*
- **[Mis à jour après remplacement du cdc] Équipements de Combat — 21 lignes (7 sets) confirmées par le cdc à jour mais pas reportées dans le code** (Bard, Journeyman, Thief [valeur corrigée], Adventurer, Knight, Shopkeeper, Soldier) — détail complet en Priorité 1. Champ éditable (donc pas un blocage AGENTS.md au sens strict), mais valeur par défaut **périmée**, à corriger manuellement dans `equipment-data.ts` (le cdc note lui-même que ce report doit se faire à la main).

### 🔧 Bug flagrant corrigé

_(aucun — hors périmètre de cette priorité, qui est une vérification d'éditabilité, pas une chasse au bug.)_

### Non vérifié

- **Pattern « même valeur sur les 6 emplacements »** pour la stat de type d'Équipement d'Expédition (cdc L.1415) — hors du champ vérifiable de cette priorité (question de vraisemblance des données, pas d'éditabilité ; les champs restent de toute façon librement modifiables ligne par ligne).

### 📋 Liste consolidée des données « reste à définir » (pour tenir à jour le cdc)

*(mise à jour après remplacement du cdc — les entrées résolues par la nouvelle version ont été retirées de cette liste)*

- **Level Up — formule Troupes Argent** — valeur par défaut actuelle en code : *aucune* (retour `null` codé en dur, pas de champ admin) — éditable : **non** ⚠️ *(toujours d'actualité, cdc à jour inchangé sur ce point)*
- **Équipements de Combat — 3 sets réellement encore inconnus** (Smuggler, Royal Archer, Royal Guard = 9 lignes, contre 5 sets/15 lignes supposés à l'audit initial) — valeur par défaut actuelle : `"Inconnu"` — éditable : oui
- **Équipements de Combat — 7 sets déjà confirmés par le cdc à jour mais pas reportés en code** (Bard, Journeyman, Thief, Adventurer, Knight, Shopkeeper, Soldier = 21 lignes, contre 5 sets/15 lignes à l'audit initial) — valeur par défaut actuelle : `"Inconnu"` (périmée) — éditable : oui, mais valeur à corriger manuellement
- **Équipement de Combat — coût de fusion en Pouciel** — aucune implémentation trouvée du tout, reconfirmé absent après relecture du cdc à jour (voir Priorité 1) — éditable : **N/A, la fonctionnalité elle-même semble absente**
- **Ranking — seuils/récompenses Bronze (vide), Or (vide), Platine (seuils incomplets, récompenses non confirmées)** — éditable : oui, UI dédiée déjà en place — *(toujours d'actualité, cdc à jour inchangé sur ce point)*
- **Simulateur d'achat de consommables** — absent du code, N/A (hors périmètre, cdc et brief cohérents entre eux sur ce point)

*(Retirées de cette liste, résolues par le remplacement du cdc : les 8 incréments par étoile d'Expédition — désormais 10/10 confirmés en Légendaire, éditables ; le coût de fusion Terradust — désormais confirmé 600/1100/2000/4000/8000, identique au code.)*

---

## Synthèse — pour action rapide

*(Mise à jour après remplacement du cdc/brief par leurs versions à jour — voir « 🔄 Mise à jour » en tête de document. Les points résolus par la seule mise à jour documentaire ont été retirés de cette liste.)*

Par ordre d'impact probable :

1. **Le bug Platine (+2 au lieu de +1 point/niveau) a été corrigé** pendant cet audit (commit `bdec46f`) — impactait potentiellement tous les joueurs Platine sur plusieurs écrans. Indépendant des docs.
2. **Classement — Bronze et Or n'ont aucune donnée** en base malgré un cdc qui les dit confirmés depuis longtemps (reconfirmé contre le cdc à jour, ce n'est pas un problème de fraîcheur documentaire) : à trancher en priorité si le calculateur Classement est réellement utilisé pour ces 2 ligues côté joueurs.
3. **Équipement de Combat — aucun coût de fusion en Pouciel n'est implémenté**, alors que la formule est verrouillée depuis longtemps dans le cdc (reconfirmé contre le cdc à jour) — à vérifier si c'est un oubli ou un report volontaire à une itération future du Simulateur d'Équipement de Combat.
4. **Équipement d'Expédition — le simulateur ne distingue jamais une pièce Légendaire (incréments vérifiés) d'une pièce d'une autre rareté (incréments extrapolés)** — écart réduit par la mise à jour du cdc (les 10 stats sont désormais confirmées, pas seulement 2), mais la nuance par rareté reste absente de l'UI.
5. Les 21 lignes d'équipement de combat désormais confirmées par le cdc à jour (Bard/Journeyman/Thief/Adventurer/Knight/Shopkeeper/Soldier) restent à reporter dans `equipment-data.ts` — seuls 3 sets (Smuggler/Royal Archer/Royal Guard) sont encore réellement inconnus.
6. Level Up Argent devrait recevoir un champ admin éditable (même vide/placeholder), conformément à la règle AGENTS.md sur les données non confirmées (reconfirmé contre le cdc à jour).

*(Retirés de cette liste — résolus par le seul remplacement du cdc/brief, aucune action requise : le coût de fusion Terradust, désormais confirmé identique au code ; la disparition de « Comparaison de stuff », désormais documentée comme une décision produit volontaire et déjà tracée dans le cdc.)*
