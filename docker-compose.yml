services:
  app:
    image: ghcr.io/magicgg91/ml-helper:dev
    pull_policy: always
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
