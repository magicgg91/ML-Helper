NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-random-secret"

# SMTP settings for the /contact form. Leave unset in development — the
# form then reports that sending isn't configured instead of failing.
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASSWORD=""
CONTACT_EMAIL=""
